pragma solidity ^0.5.0;

contract MyContract {
    struct StructType {
        uint[] contents;
        uint moreInfo;
    }
    StructType s;

    function add() public pure returns (uint) {
        uint i;
        int f;
        int g = 10;
        uint len;
        
        return i;
    }
}